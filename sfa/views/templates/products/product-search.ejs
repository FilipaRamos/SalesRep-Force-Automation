<div>
    <select id="product-selector" class="form-control selectpicker"
            data-live-search-placeholder="Procurar por nome ou categoria..." data-show-subtext="false"
            title="Procurar" data-live-search="true" data-dropup-auto="false" data-live-search-normalize="true">
        <option ng-repeat="product in productsCtrl.products"
                value="{{CodArtigo}}" data-subtext="{{productsCtrl.getCategory(product.Familia)}}"
                id="product-{{product.CodArtigo}}">
            {{product.CodArtigo}} - {{product.PVP1}} €
        </option>
    </select>
    <div>
        <button type="button" class="btn btn-primary " ng-click="productsCtrl.goToProduct()">Ver produto</button>
    </div>
</div>
