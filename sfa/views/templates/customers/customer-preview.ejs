<div class="col-sm-4 col-lg-4 col-md-4">
    <div class="thumbnail">
        <div class="caption">
            <h4 class="pull-right" ng-show="customer.vendas > 0">{{customer.vendas | currency:"€"}}</h4>
            <h4><a href="/cliente?id={{customer.id}}">{{customer.nome}}</a></h4>
            <p>{{customer.telefone}}</p>
            <p>{{customer.morada}}</p>
        </div>
    </div>
</div>

